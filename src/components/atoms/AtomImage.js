import styled from "styled-components"
import { measurements } from "../../services/utilStyles";

export const AtomImage = styled.img`
    height: 380px;
    width: 100%;
    border-radius: ${measurements.bounding.smaller};
`;

