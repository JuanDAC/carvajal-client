import styled from "styled-components"
import { colors, measurements } from "../../services/utilStyles";

export const AtomTitle = styled.h1`
    color: ${colors.foreground.primary};
    font-size: ${measurements.text.title};
    text-align: center;
    font-weight: bold;
`;

